[pytest]
addopts = -v -s -rs
markers =
    live: tests that hit real external services or a live configured database/runtime path
    kb: tests that exercise knowledge-base retrieval
    cve: tests that exercise official CVE / KEV retrieval
    osint: tests that exercise Tavily / external OSINT retrieval
    librarian: tests that exercise the librarian research pipeline
