# Archived Plan

This file is retained only as historical context from the pre-promotion `v2` rollout.

The active architecture no longer uses the old `src/v2` split, `SAIBER_GRAPH_VERSION`, or the legacy/non-legacy graph toggle described here. Current runtime structure and validation commands should be taken from the active docs in `docs/`.
